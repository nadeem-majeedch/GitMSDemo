# GitMSDemo
it's a class demo in MS Data Science.

just trying git commands like

git version

git --exec-path

git help

git help git

git help tutorial

git help everyday

git config --global user.name "Nadeem Majeed"

git config --global user.email "nadeem.majeed@pucit.edu.pk"

git config --global core.editor "code --wait"

git config --global color.ui true

git config --global --list

cd
cd Desktop/gittest
dir /a
git init


code
"Data data every where" >author.txt
notepad.exe "myfile.txt"


git status
git add myfile.txt
git status





